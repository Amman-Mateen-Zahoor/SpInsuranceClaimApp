import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Profile from '../../common/tabScreens/Profile'

const EmpProfile = () => {
  return (
  <Profile from={'empProfile'}/>
  )
}

export default EmpProfile

const styles = StyleSheet.create({})